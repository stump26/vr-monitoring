{
  "name": "@vr-monitoring/client",
  "version": "1.0.0",
  "main": "index.js",
  "license": "sanguk.park",
  "scripts": {
    "start": "yarn start:dev",
    "start:dev": "NODE_ENV=development ts-node start",
    "start:production": "NODE_ENV=production ts-node start",
    "build": "NODE_ENV=production PWD=`pwd` yarn workspace @vr-monitoring/shared-webpack run build --config $PWD/webpack.client.config.ts"
  },
  "dependencies": {
    "styled-components": "^5.2.1"
  },
  "devDependencies": {
    "@types/styled-components": "^5.1.4"
  }
}
